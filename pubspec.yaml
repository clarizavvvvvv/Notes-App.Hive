name: notes_app
description: "Elegant Notes App using Hive + BLoC (Light Blue Theme)"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0" # lebih aman dan kompatibel untuk Flutter 3.24+

dependencies:
  flutter:
    sdk: flutter

  # 🧠 State management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5

  # 📦 Local database
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # 💅 UI icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 🧹 Linting rules (code style)
  flutter_lints: ^3.0.2

  # 🛠️ Code generator for Hive
  build_runner: ^2.4.13
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
